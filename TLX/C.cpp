#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    bool lampu[100];

    for (int i = 0; i < 100; i++) {
        lampu[i] = false;

        cout << lampu[i];
    }  

    for (int p = 0; p < 10; p++) {
        
    }

    // for (int p = ...) {
    //     for (int i = ...) {
    //         if (...) {
    //             lampu[i] = ...;
    //         }
    //     }
    // }


    return 0;
}