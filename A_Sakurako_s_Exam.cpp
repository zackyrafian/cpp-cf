#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int T; cin >> T;
    while (T--) { 
        int a, b; 
        cin >> a >> b;

        int c = a + b;
        int d = a - b;
        
        cout << c << d; 


        cout << endl;
    }
    return 0;
}