#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N; cin >> N;

    if (N % 4 == 0)  {
        cout << "No";
    } else {
        cout << "Yes";
    }

    return 0;
}