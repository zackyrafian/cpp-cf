#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, t, w; cin >> n >> t >> w; 
    int r = n * t * w;
    cout << r;
    return 0;
}
