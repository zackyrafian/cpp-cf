#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    string s;
    int n; 

    cin >> s >> n;

    for (int i = 0; i < n; i++){
        cout << s;        
    }
    return 0;
}