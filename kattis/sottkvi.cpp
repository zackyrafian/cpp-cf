#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N, K, D;
    cin >> N >> K >> D;

    for (int i = 0; i < N; i++) { 
    }

    return 0;
}