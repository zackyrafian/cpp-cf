#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int B, L;
    cin >> B >> L;
    int R = 2 * min(B,L);

    cout << R << endl;
    return 0;
}